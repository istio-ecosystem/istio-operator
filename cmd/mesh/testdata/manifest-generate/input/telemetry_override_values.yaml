apiVersion: install.istio.io/v1alpha2
kind: IstioOperator
spec:
  profile: empty
  components:
    telemetry:
      enabled: true
  values:
    mixer:
      adapters:
        prometheus:
          metricsExpiryDuration: 123m


