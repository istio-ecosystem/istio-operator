apiVersion: install.istio.io/v1alpha2
kind: IstioOperator
spec:
  profile: empty
  components:
    ingressGateways:
      - namespace: istio-ingress-1
        k8s:
          resources:
            requests:
              cpu: 111m
              memory: 222Mi
      - namespace: istio-ingress-2
        values:
          type: NodePort
