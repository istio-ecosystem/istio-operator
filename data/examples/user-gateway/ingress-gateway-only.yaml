apiVersion: install.istio.io/v1alpha2
kind: IstioOperator
spec:
  profile: empty
  gateways:
    enabled: true
    components:
      namespace: my-namespace
      ingressGateway:
        enabled: true
